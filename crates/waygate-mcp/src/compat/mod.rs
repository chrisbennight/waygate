//! Gateway-owned adapters for retired or proposal-only MCP surfaces.
//!
//! Compatibility lives below this boundary so standard MCP catalog and
//! dispatch code never requires an upstream server to implement a draft.

pub mod search_tools_v1;
