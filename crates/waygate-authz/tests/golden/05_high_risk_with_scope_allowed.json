{
  "name": "delete_dataset_with_scope_allowed",
  "description": "Counterpart to the StepUpRequired case (golden 04): once the admin re-authorizes and the bearer carries mcp:invoke:high, the same delete_dataset call must succeed. Pins that the step-up forbid is the only thing gating the call.",
  "principal": {
    "sub": "alice",
    "auth_method": "oauth",
    "groups": ["mcp-admins"],
    "scopes": ["mcp:invoke", "mcp:read", "mcp:invoke:high"]
  },
  "action": {
    "kind": "CallTool",
    "name": "example-memory-assistant.delete_dataset",
    "risk": "high"
  },
  "resource": {
    "kind": "Tool",
    "server": "example-memory-assistant",
    "name": "delete_dataset",
    "risk": "high",
    "side_effects": true
  },
  "expected": {
    "decision": "Allow"
  }
}
