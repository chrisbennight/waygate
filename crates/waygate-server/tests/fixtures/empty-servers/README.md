This fixture intentionally contains no `*.yaml` manifests. It boots the gateway
with an authoritative zero-upstream catalog so Code Mode runner lifecycle tests
exercise only the gateway-owned execution process.
