[package]
name = "waygate-agent-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# The concrete implementations of waygate-agent's trait seams (model,
# dispatch, approval gate), moved out of waygate-admin.
# Domain-layer: may depend on the invocation pipeline and LLM translation;
# must never depend on waygate-admin/waygate-server (Composition).
[dependencies]
waygate-agent.workspace = true
waygate-invocation.workspace = true
waygate-llm-translate.workspace = true
waygate-mcp.workspace = true
waygate-oidc.workspace = true
waygate-upstream.workspace = true
async-trait.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
uuid.workspace = true

[dev-dependencies]
waygate-core.workspace = true
rmcp.workspace = true
